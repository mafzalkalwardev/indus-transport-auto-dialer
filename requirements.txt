PyQt6>=6.7.0
PyQt6-WebEngine>=6.7.0
pandas>=1.5.0
openpyxl>=3.0.0
Pillow>=9.0.0
pyperclip>=1.8.0
pyinstaller>=6.0.0
psutil>=5.9.0

# AI call-progress detection (Call-Progress-Analysis / Answering-Machine-Detection)
numpy>=1.23.0
scikit-learn>=1.3.0
joblib>=1.3.0
soundfile>=0.12.1      # read voice samples when (re)building the dataset
# Live speaker-loopback capture for the AI audio monitor. Optional: if it is not
# installed (or no audio device is present) the app falls back to DOM-only
# detection automatically.
soundcard>=0.4.2
